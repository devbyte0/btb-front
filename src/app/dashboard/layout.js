export const metadata = {
  title: "Dashboard",
  description: "Your Barista Training Bangladesh dashboard. Manage enrollments, track attendance, and access training resources.",
  robots: { index: false, follow: false },
};

export default function DashboardLayout({ children }) {
  return children;
}
