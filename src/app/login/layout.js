export const metadata = {
  title: "Login",
  description: "Login to your Barista Training Bangladesh account. Students, trainers, and administrators can access their dashboard.",
};

export default function LoginLayout({ children }) {
  return children;
}
